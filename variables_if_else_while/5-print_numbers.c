#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int chif;

	for (chif = 0 ; chif <= 9 ; chif++)
	{
		printf("%d", chif);
	}
	printf("\n");
	return (0);
}
